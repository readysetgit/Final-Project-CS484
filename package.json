{
  "name": "Assignment2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "grade": "npm install && cd client && npm install && npm run build && cd .. && npm run start-db-and-server",
    "start-db-and-server": "concurrently --kill-others \"npm run startdb\" \"npm start\"",
    "startdb": "json-server --watch server/db.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --ignore 'db.json' server/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.23.0",
    "express": "^4.17.1",
    "json-server": "^0.17.0"
  },
  "devDependencies": {
    "concurrently": "^6.3.0",
    "nodemon": "^2.0.14"
  }
}
